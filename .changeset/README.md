# Changesets

This project uses [changesets](https://github.com/changesets/changesets) for version management.

To add a changeset, run:

```bash
pnpm changeset
```

This will prompt you for a version bump type (patch/minor/major) and a summary of the change.
